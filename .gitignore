# Compiled binaries
ForestFireSİm
ForestFireSİm_dbg
out/

# Simulation result vectors are large binary files — excluded
simulations/results/*.vec
simulations/results/*.vci
# Scalar results (.sca) are tracked — required by submission guidelines (§8)

# OMNeT++ IDE files
.metadata/

# OMNeT++ IDE project files
.cproject
.oppbuildspec
.project
.settings/

# Auto-generated message files (regenerated by opp_msgc from .msg)
msg/*_m.cc
msg/*_m.h
